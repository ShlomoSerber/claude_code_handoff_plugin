{
  "description": "On session start, resume and /clear, print a one-line pointer to this project's latest handoff (path, age, size). Never injects the handoff body.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          { "type": "command", "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/session-start.sh", "timeout": 5 }
        ]
      }
    ]
  }
}
